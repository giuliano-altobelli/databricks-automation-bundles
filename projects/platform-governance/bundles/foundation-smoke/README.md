# Foundation Smoke Bundle

This inert bundle metadata fixture proves the monorepo foundation contracts without creating Databricks assets.

It intentionally has no `databricks.yml`, SQL, UDF source, ABAC policy, or access-map contract.
